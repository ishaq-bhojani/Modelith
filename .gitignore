node_modules/
out/
dist/
test-results/
playwright-report/

# SDD scratch workspace (ledger, briefs, review packages)
.superpowers/

# Never commit scratchpad scratch files, including mangled absolute paths
*scratchpad*
release/

# CI build log, tee'd so the icon check can inspect electron-builder output
dist.log
